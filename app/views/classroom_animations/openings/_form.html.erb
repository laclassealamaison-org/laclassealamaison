<%= form_with(url: classroom_animation_openings_path(session), local: true) do |form| %>
  <div class="mb-2">
    <%= form.submit 'Ouvrir la salle de classe aux élèves', class: 'btn-green' %>
  </div>
<% end %>
